# HYMessage iOS App 运行指南

## 📱 运行步骤

### 方法一：使用 Xcode（推荐）

1. **打开项目**
   ```bash
   open HYMessage.xcodeproj
   ```
   或者在 Finder 中双击 `HYMessage.xcodeproj` 文件

2. **选择运行目标**
   - 在 Xcode 顶部工具栏，点击设备选择器
   - 选择：
     - **iOS 模拟器**（推荐用于测试，如 iPhone 15 Pro）
     - 或连接的 **真实 iOS 设备**（需要开发者账号）

3. **运行项目**
   - 点击左上角的 **▶️ 运行按钮**（或按快捷键 `⌘R`）
   - 等待编译完成，应用会自动启动

### 方法二：使用命令行

1. **检查 Xcode 命令行工具**
   ```bash
   xcodebuild -version
   ```

2. **编译项目**
   ```bash
   xcodebuild -project HYMessage.xcodeproj \
             -scheme HYMessage \
             -sdk iphonesimulator \
             -destination 'platform=iOS Simulator,name=iPhone 15 Pro' \
             build
   ```

3. **运行模拟器并安装应用**
   ```bash
   xcrun simctl boot "iPhone 15 Pro" 2>/dev/null || true
   xcodebuild -project HYMessage.xcodeproj \
             -scheme HYMessage \
             -sdk iphonesimulator \
             -destination 'platform=iOS Simulator,name=iPhone 15 Pro' \
             run
   ```

## ⚙️ 系统要求

- **macOS**: macOS 12.0 或更高版本
- **Xcode**: Xcode 15.0 或更高版本
- **iOS 部署目标**: iOS 17.0 或更高版本
- **Swift**: Swift 5.0+

## 🔧 常见问题

### 1. 编译错误：找不到文件
**问题**: `MessageModel.swift` 或其他文件找不到

**解决**: 
- 在 Xcode 中，右键点击项目 → "Add Files to HYMessage"
- 选择缺失的文件添加到项目中

### 2. 签名错误
**问题**: Code signing error

**解决**:
- 在 Xcode 中，选择项目 → Target "HYMessage" → Signing & Capabilities
- 取消勾选 "Automatically manage signing"（如果使用模拟器）
- 或配置开发者账号和证书（如果使用真机）

### 3. 模拟器无法启动
**问题**: Simulator 启动失败

**解决**:
```bash
# 列出可用模拟器
xcrun simctl list devices

# 启动特定模拟器
xcrun simctl boot "iPhone 15 Pro"
```

### 4. 应用崩溃
**问题**: 应用启动后立即崩溃

**解决**:
- 检查 Xcode 控制台的错误信息
- 确保所有文件都已正确添加到项目中
- 清理构建：Product → Clean Build Folder (⇧⌘K)

## 📋 项目结构检查清单

运行前确保以下文件存在：

```
HYMessage/
├── HYMessage/
│   ├── HYMessageApp.swift          ✅
│   ├── Models/
│   │   └── MessageModel.swift      ✅
│   ├── Services/
│   │   ├── MessageManager.swift    ✅
│   │   └── MessageClassifier.swift ✅
│   ├── Views/
│   │   ├── ContentView.swift       ✅
│   │   ├── CategoryView.swift      ✅
│   │   └── MessageDetailView.swift ✅
│   ├── Assets.xcassets/            ✅
│   └── Info.plist                  ✅
└── HYMessage.xcodeproj/            ✅
```

## 🎯 功能测试

运行成功后，你应该能看到：

1. **主界面** - 4个标签页：
   - 全部短信
   - 签名
   - AI分类
   - 设置

2. **模拟数据** - 应用会自动加载12条模拟短信

3. **功能验证**:
   - ✅ 点击"全部"标签，查看所有短信
   - ✅ 点击"签名"标签，查看按签名分组的短信
   - ✅ 点击"AI分类"标签，查看AI分类结果
   - ✅ 点击任意短信，查看详情并测试分类功能

## 💡 提示

- **首次运行**: 可能需要等待 Xcode 下载和安装模拟器运行时
- **性能**: 模拟器运行可能比真机慢，这是正常的
- **调试**: 使用 Xcode 的调试器可以设置断点和查看变量值

## 🚀 下一步

项目运行成功后，你可以：
- 修改模拟数据（在 `MessageManager.swift` 的 `generateMockMessages()` 方法中）
- 调整UI样式和颜色
- 添加新功能
- 集成真实的短信读取功能（需要系统权限）

---

如有问题，请检查 Xcode 控制台的错误信息，或查看 README.md 获取更多信息。

